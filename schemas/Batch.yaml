type: object
required:
  - skuId
  - defaultLabTestingState
  - approvalStatus
  - approvalStatusDateTime
lint:
  csv: true
properties:
  defaultLabTestingState:
    type: string
    enum: ["NotSubmitted"]
    description: Uses same name as Metrc DefaultLabTestingState
  approvalStatus:
    type: string
    enum: ["Approved"]
    description: Uses same name as Metrc ApprovalStatus
  approvalStatusDateTime:
    type: string
    format: date-time
    description: Uses same name as Metrc ApprovalStatusDateTime
  cannabinoids:
    $ref: ./content/Cannabinoids.yaml
  terpenes:
    $ref: ./content/Terpenes.yaml